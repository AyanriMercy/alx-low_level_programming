#include "main.h"

/**
 * main -  test that the function positive_or_negative() gives the
 * correct output when given a case of 0
 * Description: a file
 * Return: 0
 */

int main(void)
{
	int num;

	num = 0;
	positive_or_negative(num);

	return (0);
}
